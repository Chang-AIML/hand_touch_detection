# Tested with torch 2.9.1+cu128 / torchvision 0.24.1+cu128 (conda env `base`).
# Install torch/torchvision matching your CUDA from https://pytorch.org first.
torch>=2.6
torchvision>=0.21
numpy
h5py
pillow
pandas
tqdm
matplotlib      # spot_head PR-curve plots (lazy-imported in common/score.py)
tabulate        # spot_head eval tables (train_head, common/score, common/eval)
