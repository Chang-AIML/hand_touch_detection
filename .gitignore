# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / archive junk
.DS_Store
**/._*
__MACOSX/
*.zip

# model weights / checkpoints (regenerate by training)
*.pt
*.pth

# large regenerable feature arrays (TSP [N,512], V-JEPA per-frame) + MViT GVF
*.npy
*.h5

# per-epoch intermediate VAL predictions (selection artifacts, regenerable+bulky);
# final pred-test.* + eval tables + loss curves + logs are kept.
outputs/**/pred-val.*.recall.json.gz
outputs/**/pred-val.*.json

# everything else under outputs/ (predictions, eval tables, logs, GVF, loss curves)
# IS version-controlled -- only the weights/features above are excluded.

# local-only scratch docs (README.md is the single source of truth) + stray downloads
HANDOFF.md
STRUCTURE.md
val (1).json
