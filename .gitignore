# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / archive junk
.DS_Store
**/._*
__MACOSX/
*.zip

# model weights / checkpoints (regenerate by training)
*.pt
*.pth

# large regenerable feature arrays (TSP [N,512], V-JEPA per-frame)
*.npy


# pipeline outputs are regenerable; only curated deliverables under
# outputs/spot_head/best/ are version-controlled.
outputs/*
!outputs/spot_head
outputs/spot_head/*
!outputs/spot_head/best
